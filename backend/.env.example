DATABASE_URL=postgresql://postgres:password@localhost:5432/dressedup
SECRET_KEY=your-secret-key-here-generate-with-openssl-rand-hex-32
DEBUG=True
ENV=development
ALLOWED_ORIGINS=http://localhost:8081,http://localhost:19006
RUN_MIGRATIONS_ON_STARTUP=false

# Morning push scheduler (ticks every minute when enabled). Free via Expo Push API.
NOTIFICATION_SCHEDULER_ENABLED=false
# Optional Expo access token for higher push rate limits (not required for dev).
EXPO_ACCESS_TOKEN=

# Email receipt import (Mailgun inbound). Each user gets u-{token}@EMAIL_INGEST_DOMAIN.
EMAIL_INGEST_ENABLED=false
EMAIL_INGEST_DOMAIN=
MAILGUN_WEBHOOK_SIGNING_KEY=
# Allow POST /closet/email-ingest/simulate in production (off by default).
EMAIL_INGEST_ALLOW_SIMULATE=false

# Trip weather (Open-Meteo — free, no API key). Used for date-based packing lists.
WEATHER_API_ENABLED=true
WEATHER_API_TIMEOUT_SEC=12

# Image storage — local dev (default). Production: set STORAGE_PROVIDER=s3.
STORAGE_PROVIDER=local
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_REGION=us-east-1
# S3_BUCKET=dressedup-media-prod
# S3_PUBLIC_BASE_URL=
# S3_ENDPOINT_URL=
# S3_OBJECT_ACL=

# Set to "anthropic" and provide a key to use the real model (costs ~$0.002/scan).
VISION_PROVIDER=stub
ANTHROPIC_API_KEY=
VISION_MODEL=claude-haiku-4-5

# LLM stylist — personalized outfit notes + shop gap insights (reuses ANTHROPIC_API_KEY).
# Set STYLIST_PROVIDER=anthropic to enable; falls back to rule-based hints when off.
STYLIST_PROVIDER=stub
STYLIST_MODEL=claude-haiku-4-5
STYLIST_MAX_OUTPUT_TOKENS=400

# Garment background removal (rembg/ONNX, runs locally — no API cost).
# Cutouts are saved as transparent PNGs and used as thumbnail_url for closet
# cards and the 3D avatar fabric texture. Set false to skip (e.g. low-memory hosts).
BG_REMOVAL_ENABLED=true
BG_REMOVAL_MODEL=u2netp
BG_REMOVAL_MAX_PX=1024
